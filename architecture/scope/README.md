# Project Scope Documents

_[← Repository README](../../README.md) · [Enterprise architecture](../README.md)_

One document per delivered (or in-flight) initiative, numbered
chronologically. While the [EA docs](../README.md) describe the
**current** state of the system, each scope document describes one
**change**: what plateau it started from, what it delivered, and what it
deliberately left out.

**ArchiMate viewpoint:** Implementation & Migration (Work Package,
Deliverable, Plateau, Gap).

## The EA-first change process

Every change in requirements follows the same order — the same order the EA
folders are numbered in:

1. **Align the EA first.** Walk the layers top-down and record what the
   change means for each: [1_strategy](../1_strategy/README.md) (does it
   serve an existing goal, or introduce a new driver?) → business (a `Gap`
   row on the front door today) → [3_information](../3_information/README.md)
   (new or changed data objects, flows, storage?) →
   [4_application](../4_application/README.md) (which services, components,
   interfaces change?) → technology (a `Gap` row today). Update the affected
   EA documents in the same change. If the change adds or modifies a
   stakeholder, driver, goal or principle, the initiative becomes strategy
   discovery first, ending at **Direction** approval; implementation follows
   as a separate initiative.
2. **Document the scope.** Add the next-numbered file to this folder
   describing plateaus, work packages, in and out of scope, gaps, and gate
   approvals — before implementation starts, refined as it proceeds.
3. **Pass the gate.** Before any code, the Requester approves the strategy
   and information changes (**Understanding**). At Depth 1 that is the only
   gate an ordinary change meets; **Direction** belongs to discovery and to
   the roadmap. Each gate granted is recorded in the scope document's
   Approvals table — who approved, when, and what was shown. One that was not
   granted gets no row: the table records what happened, not a census of what
   did not. An approval can be granted in the conversation or in a reply on
   the pull request.
4. **Implement.** Only then write the code, keeping the scope document and
   EA docs in sync with what is actually delivered.

An interpretation the agent adopted is recorded where it applies, in that
row's `Source` cell as `adopted — <the call>`, and the document stays `◐`: a
later word from the Requester overrides it, which an approved fact does not.
A question reaches the Requester only when the answer changes what gets built
now and nothing in the model settles it. Single consequential calls smaller
than an initiative are in [the decisions index](../decisions/README.md).

**A merged scope document is never rewritten**: it is the record of what was
approved on a date and against what information. When the current-state
documents drift from reality after a run of initiatives, restating them is
its own initiative with its own Understanding.

## Initiatives

| #   | Scope document | Delivered as | Summary |
| --- | --------------- | ------------ | ------- |
| 1   | [1_curriculum-poc.md](./1_curriculum-poc.md) | the working branch (in flight) | The PoC: generic metamodel-driven repository on DuckDB with the higher-education pack, CSV ingestion, browse and edit, metamodel manager and a grounded agent, on the curriculum slice of the institution's content |
| 2   | [2_generated-views.md](./2_generated-views.md) | the working branch (built, demo pending) | Generated architecture views in archreator-style Mermaid, agent answers as documents with diagrams, and a draft draw.io export over the same view model; no hand diagramming |
| 3   | [3_admin-notation-and-arrangeable-views.md](./3_admin-notation-and-arrangeable-views.md) | the working branch (built; demo pending) | Notation editor with live preview and domain colours as pack data; draggable shapes on generated views exported to draw.io without saving; the Ask page view-first with copyable Markdown; grouped, overlap-free graphs; roles documented, none enforced |
| 4   | [4_branches-and-target-state.md](./4_branches-and-target-state.md) | `main` (in flight) | Model branches as overlays merged with base versions; current versus target state on every artefact, analysed by work package |
| 5   | [5_propose.md](./5_propose.md) | `main` (in flight) | Propose: a document, file or link becomes a branch through an agent that links existing elements, adopts new ones and pushes back when the sources are insufficient; the Proposal Template |
| 6   | [6_search-bulk-edit-and-health.md](./6_search-bulk-edit-and-health.md) | `main` (built 2026-09-06; demo pending) | Word-by-word search over names, descriptions and attributes with ranking; bulk edit from Browse; the Health page with freshness per source and completeness per type |
| 7   | [7_roles-and-review.md](./7_roles-and-review.md) | `main` (built 2026-09-06; demo pending) | Roles enforced (Admin, Architect, Reviewer, Reader, Agent) with a debug persona switcher locally; review before merge with reviewers per element type; the business and technology layers written, views at the top of every layer document |
| 8   | [8_two-gates-and-readable-views.md](./8_two-gates-and-readable-views.md) | branch `claude/archreator-validation-feedback-gt00b7` | archreator 0.3 applied: two gates, no row for a gate that was not granted, the open-questions log retired. The glyphs distinguish the element types, so 273 nodes drop the stereotype and every document opens with a legend; nine documents stop stacking their diagrams ahead of their tables. **No claim about the repository changes** |
| 9   | [9_import-template.md](./9_import-template.md) | the working branch | Import page download of a generic CSV contract template archive for prospective adopters |
| 10  | [10_markdown-editor-and-preview.md](./10_markdown-editor-and-preview.md) | the working branch | Markdown authoring controls and preview for Markdown-capable fields, with Mermaid rendering in reader views |
| 11  | [11_gui-review-grey-theme-and-editor-modes.md](./11_gui-review-grey-theme-and-editor-modes.md) | the working branch | GUI review: editor Edit/Split/Preview modes, grey theme, process-ordered actions, one destination field each on Propose, current state as the one indicator, persona cleanup, reader-view Mermaid and Impact selector fixes |
| 12  | [12_navigation-flow-and-screenshot-order.md](./12_navigation-flow-and-screenshot-order.md) | the working branch | Navigation grouped into Home, Discover, Contribute and Manage, with README screenshots ordered to match |
| 13  | [13_same-application-on-databricks.md](./13_same-application-on-databricks.md) | branch `claude/repo-assessment-gaps-7wz42s` (built 2026-09-09; the workspace run pending) | The roadmap's step 2: the store written once on SQL with a DuckDB and a Databricks engine on the same DDL, the unit suite run on both; the deployment bundle (schema, app, warehouse, the grant step); the forwarded user's groups read from the workspace |
| 14  | [14_store-on-lakebase.md](./14_store-on-lakebase.md) | branch `claude/databricks-lakebase-compat-2s5ljj` (built 2026-09-11; the workspace run pending) | Lakebase, not the lakehouse: plateau `PLAT2` restated on the platform's Postgres, the warehouse engine replaced by a Lakebase engine on the same shared SQL store and proved on a real Postgres in every run, the bundle rebuilt around a Lakebase instance, the grant step gone |
| 15  | [15_metamodel-versions-and-organisations.md](./15_metamodel-versions-and-organisations.md) | branch `claude/metamodel-extensibility-versions-uubzua` (built 2026-09-19; Understanding presented on the pull request) | The metamodel extensible in every element and attribute; a pack kept in versions with a draft, published and retired lifecycle; organisations as a partition of the one store, one of them the default, each applying one version, so a change is tried on a copy of the real content and not in a second environment; rows deleted from the metamodel's lists; generated views drawn flat with the layer colours named above them; an element's attributes read in the groups the metamodel declares |
| 16  | [16_prepared-for-real-content.md](./16_prepared-for-real-content.md) | branch `claude/metamodel-improvements-next-steps-aesadj` (built 2026-09-20; Understanding granted in the session) | Prepared for real content: `ASM6` restated with today's size and the capacity the application has been assessed for rather than a forecast of the institution's estate; that capacity declared in one place, read by the six services that used to read the whole model, shown on the Health page and held by the suite; the traversal unwrapped from the in-process graph the store made unnecessary; an attribute's group a declared vocabulary picked from a list rather than typed; and a second worked pack, the ArchiMate 3.2 core, carried by the same engine in an organisation of its own |
| 17  | [17_import-hardening-and-content-export.md](./17_import-hardening-and-content-export.md) | branch `claude/import-module-improvements-wgrfgm` (built 2026-09-20; Understanding granted in the session) | The import module hardened for real content — a links file that adds instead of deleting, endpoints and link owners resolved in one read rather than one per row, an issue list counted in full and kept in part, a report that says how many rows were new and how many were overwritten, a configurable field separator that names itself when it is wrong, and a mapping of your own on the screen as well as the command line — and `ASVC3` widened to extraction: the organisation and branch written back out as the same three files, elements as one wide file, re-importing onto the same elements and the same edges; and one rule for what a row is called — a prefix that keeps two systems numbering from one apart, and a choice of merging a source on its `id` or on the key its own system knows the row by |
| 18  | [18_source-feeds-and-import-history.md](./18_source-feeds-and-import-history.md) | branch `claude/import-module-improvements-wgrfgm` (in flight; Understanding granted in the session) | Source feeds and import history: a staging table per source in the store's own database read through the same pipeline, a deletion indicator that retires by default and deletes only where a source is configured to, a feed that writes to a branch or to `main` as its configuration says, and a record of every run with the before-images that make one reversible. Built so far: the deletion indicator, the staging schema and the feed runner, the feed's configuration and the Feeds page with a schedule anybody can read. The run history and reversal remain |
| 19  | [19_the-product-not-the-phase.md](./19_the-product-not-the-phase.md) | branch `claude/enterprise-arch-motivation-ilfgqo` (documents only; Direction pending) | Strategy discovery: the motivation layer restated as the product's rather than a phase's. `DRV1` corrected to *a modelling problem and a data problem*; `ASM10` says plainly why a standard EA product answers only half of it; `G6` **the model is served, not exported** and `G7` **a source is connected, not piped** name the two mechanisms that follow, with the Model Context Protocol on both sides — a tool server out, a connected source in. `G4`, `OUT1` and `OUT2` retired, `P7` and `PLAT1` restated without the phase, `GAP20` opened; and every document outside the coded test scenario stops naming the organisation it was built for |
| 20  | [20_browse-filters-and-branch-safety.md](./20_browse-filters-and-branch-safety.md) | branch `claude/enterprise-arch-assessment-iln3rz` (built 2026-09-21; Understanding granted in the session) | Browse and branching assessed against the tool they are meant to replace, and the two modules' defects fixed before their features were widened. What narrows a list became one object every layer names, so a search takes several types, several statuses, both states, the work package, the source, the lifecycle, an attribute by name and value and an updated-since date — and the store ranks the whole result set before it pages, so the best match reaches the first screen at any size and the count is the answer rather than the page. The criteria are named on the screen and carried in the address, and the result set comes out as CSV from the page and as JSON or CSV from `ea find`. Eight defects with it: a search ranked over an alphabetical prefix, a drill-down that reported a false total, a partial merge that wrote a relationship without its ends, a row `main` had deleted coming back silently, a closed branch still accepting writes, the reader's own SQL answering from `main` while they stood on a branch, a feed configurable by any role on the command line, and a bulk edit that emptied an attribute when its box was left blank |
| 21  | [21_metamodel-identity-renaming-and-starters.md](./21_metamodel-identity-renaming-and-starters.md) | branch `claude/enterprise-arch-assessment-iln3rz` (built 2026-09-22; Understanding granted in the session) | A metamodel's identifier made meaningless so its name is free to change. The key of `meta_pack`, of the five `meta_` tables under it and of every organisation that applies one was a readable slug that read like a name, so correcting a framework's name left the key describing something that no longer existed; it is now opaque and permanent, minted once and carried by the pack file, so a migrated store and a freshly seeded one land on the same key. The name left what a published version freezes, because a freeze is on what a version *defines* and a label is not a definition — so a name is corrected in place at any point in a version's life. A person names a version by that name or by a unique prefix of its identifier, and the screens and the command line show the name and the version rather than the key. And the packs the repository ships are offered on a screen as starters, each beginning a new, empty organisation on the one picked, never re-pointing the one the reader is in. With them, the Manage lists reordered to the order a metamodel is read — domains, element types, relationship types, attributes, attribute groups — which is a screen detail and took no gate |
| 22  | [22_proposal-templates-revisions-and-impact.md](./22_proposal-templates-revisions-and-impact.md) | branch `claude/great-franklin-30i81n` (built 2026-09-24; Understanding granted in the session) | Propose widened from one template and one pass to any template and a design specified on its branch: a template read from the metamodel's own names and declared per organisation, with an ArchiMate reference template beside the ArchiMate Core; a revised page updating what the last pass wrote; the impact of a change set — what depends on what it changes, what it leaves dangling, who must review it — before Apply and at review, with the proposal and a generated view on the branch. With it, four defects fixed directly: the reader's finding dropped, a revised page duplicated, one Ask conversation shared by a whole organisation, and a merge that could stop half-way |
| 23  | [23_the-enterprise-level.md](./23_the-enterprise-level.md) | branch `claude/great-franklin-30i81n` (documents only; Direction granted in the session) | Strategy discovery: principle `P9`, **an element earns its place by its relationships** — upward to the business or strategy it serves, outward to something beyond its own system; a system's inside, in C4 terms its components and code, is linked from the system rather than modelled |
| 24  | [24_proposals-refined-in-conversation.md](./24_proposals-refined-in-conversation.md) | branch `claude/great-franklin-30i81n` (built 2026-09-24; Understanding granted in the session) | A proposal refined in conversation: the assistant settles the change top-down, asks about what it cannot settle — which element a name means, which type or relationship a row needs, what becomes of what depends on something retired, whether a part belongs at the enterprise level — a few questions at a time with the choices the model allows; the draft is kept between sittings and the conversation stays with the proposal. The boundary of `P9` as metamodel data; a Guide page for each persona; the assistant's model served by Databricks Model Serving |
| 25  | [25_deep-dives-settled-in-conversation.md](./25_deep-dives-settled-in-conversation.md) | branch `claude/beautiful-hamilton-vci1sy` (built 2026-09-25; Understanding granted in the session) | A deep dive beside the quick answer on Ask: the assistant settles a brief with the reader in conversation — what the analysis is about, which of five kinds (impact, landscape, transition, information flow, model quality), how far it reaches and what it is for — then weighs the maturity of every element it rests on and where an element and its documentation disagree, and hands out a styled PDF with a draw.io diagram per view, in one ZIP. A deep dive is catalogued and kept, linked from every element it cites, rated one to five stars by the people who read it, and weighed by the next deep dive on the same elements (decision 0024); reading the pages an element links to is left for later (`GAP28`) |
| 26  | [26_drawings-returned-and-the-model-connected.md](./26_drawings-returned-and-the-model-connected.md) | branch `claude/admiring-bell-wj7ocn` (the draw.io stamp built 2026-09-26; the rest pending Understanding) | The last changes before Databricks: a draw.io drawing an architect kept and drew on handed back on Propose, what the application drew known by its stamp and what a person added typed from its shape or asked about, nothing deleted unless the architect says so (decision 0026, superseding 0005's one-way export); the model's read tools served to an architect's own agent over the Model Context Protocol (`GAP7`); and the assistant reading the organisation's connected systems — the pages an element links to, the system that masters it — as the reader, cited as theirs and never written into the model (`GAP28`) |
